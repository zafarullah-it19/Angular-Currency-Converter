import { CurrencyconvPipe } from './currencyconv.pipe';

describe('CurrencyconvPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyconvPipe();
    expect(pipe).toBeTruthy();
  });
});
